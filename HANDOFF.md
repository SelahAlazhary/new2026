# 📋 Project Handoff — منصة «إيمان زيدان» للعلوم الشرعية

> انسخ هذا الملف كامل في أول رسالة بأي شات جديد ليكمل المساعد من حيث انتهينا بدون فقد أي سياق.

---

## 1) نظرة عامة
منصة تعليمية عربية (RTL) لمعلّمة العلوم الشرعية **الأستاذة إيمان زيدان** (التفسير، الحديث، الفقه الشافعي، الفقه الحنفي، التوحيد، الميراث). تشمل:
- **صفحة هبوط** عصرية قابلة للتخصيص بالكامل من الأدمن.
- **لوحة أدمن** كاملة (تحكّم في كل شيء).
- **بوابة طالب** (كورسات، مشغّل فيديو، اختبارات، بث، إشعارات، مساعدة).

**المسار:** `C:\Users\pc\Downloads\jawa-elmanhag-landing`
**التقنيات:** Next.js **16.3.2** (App Router, Turbopack) · React **19** · TypeScript · Tailwind v3 · Framer Motion · Lucide + رسوم SVG يدوية.

**النشر (Production):**
- الموقع المنشور: **(لم يُنشر بعد — سيُضاف رابط فيرسل الجديد هنا)**
- مستودع GitHub: **(مستودع جديد مستقل — يُضاف هنا)**
- مشروع Vercel: **جديد ومستقل** — نشر تلقائي عند الدفع لـ GitHub.
- الأسرار كلها في **Vercel Environment Variables** (وفي `.env.local` محلياً) — راجع `DEPLOY.md`.

---

## 2) التشغيل
```bash
cd "C:/Users/pc/Downloads/jawa-elmanhag-landing" && npm install && npm run dev
```
- المتصفّح: `http://localhost:3000` (في التطوير) — كنا نختبر على المنفذ `3300` عبر `npm start -- -p 3300` بعد `npm run build`.
- **دخول المالك (أدمن):** يُضبط من `ADMIN_EMAIL` و`ADMIN_PASSWORD` في `.env.local` قبل أول تشغيل — لا توجد بيانات دخول متحوتة في الكود.
- الطلاب يسجّلون من `/register` (تفعيل فوري بدون موافقة إدارة).

---

## 3) طبقة البيانات (القاعدة الفعلية = Firebase Realtime DB · والملف المحلي احتياطي للتطوير)
- **القاعدة الحقيقية في الإنتاج: Firebase Realtime Database** (تفاصيلها في §5-ط و§5-ي). إذا لم تُضبط متغيّرات Firebase في `.env.local` يسقط النظام تلقائياً إلى **ملف JSON محلي** `data/db.json` (يُنشأ فارغاً عند أول تشغيل — احذفه لإعادة التهيئة). ← هذا ما يجعل أي نسخة جديدة تشتغل محلياً فوراً بلا إعداد.
- كل الوصول معزول خلف `lib/db.ts` + `lib/store.ts` (`loadDB/getDB/saveDB/getScopedDB/redeemCode/gradeQuiz` + مساعدات المستخدمين/الأكواد). `lib/firebase.ts` يتكفّل بالمزامنة السحابية.
- **المنصة تبدأ فارغة تماماً** (لا بيانات وهمية): لا صفوف/مواد/طلاب/أكواد/اختبارات/بث. الموجود فقط: حساب الأدمن + نصوص الواجهة القابلة للتعديل.
- الملفات المرفوعة تُخزَّن في `public/uploads/` وتُخدَم عبر **`/api/file/[name]`** (مهم: خادم الإنتاج لا يخدم ملفات public المُضافة وقت التشغيل، لذلك نخدمها عبر API — مع دعم Range للفيديو).

### أهم الأنواع (`lib/types.ts`)
- `SiteContent` (المحتوى القابل للتعديل): brand, platformSubtitle, teacher{name,subject,headline,tagline,bio,avatar,logo,rating,ratingCount,topStudents}, hero{statusPill,frame}, whatsapp, social, **support{email,phone,whatsapp}**, url, theme{layout,preset,customPrimary}, **termEnd, termPrice**, grades[], features[], curriculum[], honorStudents[], faqs[], **cta{registerLabel,registerUrl,heroPrimaryLabel,secondaryLabel,videoUrl}**, **ui: Record<key, {hidden?, fill?, text?}>**.
- `Subject`: id,name,teacher,grade,**track("علمي"|"أدبي"|"الكل")**,lessons,students,**price(=السعر الشهري)**,cover?,videos:Lesson[],materials?:Material[],status.
- `Lesson`: id,title,url,duration?,isFree?.  `Material`: id,title,url.
- `Code`: code, subjectId ("*" لكود الترم), subjectName, **plan("ترم"|"شهر")**, status, student?, createdAt.
- `User`: id,name,role,username(بريد),passwordHash,salt,active,phone?,grade?,track?,**gender?("male"|"female")**,school?,governorate?,progress?,enrolled?(قديم),**subscriptions:Subscription[]**,createdAt.
- `Subscription`: id, subjectId("*"=كل المواد), plan, activatedAt, **expiresAt(null=دائم)**.
- `Notification`: id,title,body,createdAt,grade?,track?.
- كيانات أخرى: `GradeRow`, `Exam`(+url?), `Live`(+url?), `Ticket`.

---

## 4) نظام الخطط والاشتراكات (الحالي — مهم جداً)
- **الخطط تُدار من `/admin/plans`**: لكل خطة **اسم** حرّ + **نوع** (`term` ترم كامل / `month` شهري / `custom` مخصّص) + **نطاق** (`all` كل المواد أو `subject` كورس محدّد) + سعر + مدّة + مزايا + شارة + إظهار/إخفاء + ترتيب.
- **حساب مدّة الاشتراك** كلّه في `lib/access.ts` → `planExpiry()`:
  - `term` → ينتهي في `plan.endsAt` (أو `content.termEnd` العام) — نهاية اليوم، وإلا `durationDays` (١٢٠ يوماً افتراضياً).
  - `month` → `durationDays` (٣٠ يوماً افتراضياً).  ·  `custom` → `durationDays` (٠/فارغ = بلا انتهاء).
- **اشتراك الترم الكامل** = اشتراك بنطاق `subjectId="*"` **للطالب صاحبه وحده**، يفتح كل المواد المتاحة لصفّه/شعبته، و**ينتهي تلقائياً** عند `expiresAt` (الأقفال تعود فوراً: الروابط والملفات والاختبارات والتقدّم).
- **الأكواد** (`/admin/codes`) تُولَّد **من خطة**: يحمل الكود `planId` ومنه تُشتق المدّة والنطاق. كل كود **لمرّة واحدة** ويُسجَّل عليه `studentId` و`usedAt`.
- منطق الوصول: `courseActive` · `lessonActive` · `activeSubs` · `hasTermAccess` · `termExpiry` · `subscriptionFor` · `daysLeft` · `eligibleFor`.
- redeem في `lib/db.ts` → `redeemCode()` (يتحقّق من الحساب، الصف/الشعبة، انتهاء الخطة) + مسار `POST /api/redeem`.
- الأكواد القديمة (بلا `planId`) تعمل عبر خطة ضمنية للتوافق.

## 5) API (كلها Node runtime, force-dynamic)
- `GET /api/content` — **حمولة مقيّدة بدور صاحب الجلسة** (`getScopedDB`):
  - **زائر**: المحتوى + الخطط الظاهرة + أسماء الكورسات فقط (بلا دروس/روابط/ملفات) — ولا مستخدمين ولا أكواد ولا إشعارات ولا اختبارات/بث، **ولا رابط الدرس المجاني**.
  - **طالب**: حسابه هو فقط + كورسات صفّه/شعبته + **روابط الدروس المفتوحة له فقط** + إشعاراته + الاختبارات/البث. **الأكواد لا تُرسل إطلاقاً**، و**الإجابات الصحيحة للاختبارات تُحذف** قبل الإرسال.
  - **أدمن**: كل شيء.
- `PUT /api/content` — دمج تعديل (أدمن فقط، دمج عميق للـcontent شامل cta/support/plansSection).
- `GET /api/free-lesson` — رابط الدرس المجاني **للمسجّلين فقط** (٤٠١ للزائر).
- `POST /api/quiz` — تسليم إجابات اختبار درس؛ **التصحيح على السيرفر** ويُرفض إن كان الدرس غير مُفعّل.
- `POST /api/notifications/read` — تعليم إشعارات الطالب كمقروءة.
- `POST /api/upload` — رفع صور (أدمن) · `GET /api/file/[name]` — خدمة الملفات مع دعم Range.
- `POST /api/auth/login` · `logout` · `GET /api/auth/me` — جلسات HMAC (`emz_session`).
- `POST/PATCH/DELETE /api/users` · `POST /api/redeem` · `POST /api/progress` (الصلاحية من الاشتراك الساري لا من `enrolled`).

## 5-ب) تكامل Google Meet (البث المباشر)
- **الأسرار في `.env.local` فقط** (مُستثنى في `.gitignore`): `GOOGLE_CLIENT_ID` · `GOOGLE_CLIENT_SECRET` · `GOOGLE_REDIRECT_URI` · `AUTH_SECRET`. لا شيء منها في الكود ولا يصل المتصفّح.
- `lib/google.ts`: تدفّق OAuth كامل (authUrl/exchange/refresh/revoke) + `createMeet()` عبر Google Calendar API (`conferenceData.createRequest`) + `deleteMeetEvent()`. النطاقات: `calendar.events` + `openid email` فقط.
- المسارات (أدمن فقط): `GET /api/google/auth` (يبدأ الربط، مع state في كوكي httpOnly ضد CSRF) · `GET /api/google/callback` · `POST /api/google/meet` (ينشئ الاجتماع + جلسة البث) · `DELETE /api/google/meet` (يحذف الجلسة وحدثها من التقويم) · `POST /api/google/disconnect` (يُلغي الرموز عند جوجل ويمسحها).
- الرموز تُحفظ في `db.integrations.google` ولا تُرسل أبداً: `publicIntegrations()` تُخرج `{connected, email, connectedAt, configured}` فقط، وللأدمن حصراً. و`PUT /api/content` يرفض تعديل `integrations`.
- **عنوان العودة يُشتق تلقائياً من عنوان التشغيل** (أي منفذ) ما لم يُضبط `GOOGLE_REDIRECT_URI`، و**يُعرض في بطاقة الربط داخل `/admin/live` مع زر نسخ** لتسجيله كما هو في Google Cloud — هذا حلّ الخطأ 400 redirect_uri_mismatch.
- **إعداد مطلوب في Google Cloud Console**: تفعيل *Google Calendar API* · إضافة عنوان العودة حرفياً في *Authorized redirect URIs* · إضافة حساب المعلّمة كـ Test user طالما شاشة الموافقة في وضع Testing.

## 5-د) استضافة الوسائط على Google Drive (رفع في الخلفية)
- **ملاحظة مهمة:** Blogger لا يوفّر واجهة رفع وسائط (الرفع داخل محرّره فقط، وواجهة Picasa التي كان يعتمد عليها أُغلقت ٢٠١٩)، فنُفِّذت الفكرة نفسها على **Google Drive** بنفس الحساب المربوط.
- `uploadToDrive()` في `lib/google.ts`: ينشئ مجلّداً باسم «منصة إيمان زيدان — الوسائط» (مرة واحدة، ويُحفظ معرّفه)، يرفع الملف رفعاً متعدّد الأجزاء، يمنحه صلاحية «من لديه الرابط: قارئ»، ويعيد رابط عرض: صور عبر `lh3.googleusercontent.com/d/{id}` وفيديو/مستندات عبر `drive.google.com/file/d/{id}/preview`.
- النطاق المضاف: **`drive.file`** فقط — لا يصل التطبيق لأي ملف آخر في الحساب. **يتطلّب إعادة ربط حساب جوجل مرة واحدة** لاعتماد النطاق الجديد.
- الوجهة تُضبط من «تخصيص الموقع ← الصور»: `content.mediaHost = "local" | "drive"`، ويمكن فرضها لكل طلب بحقل `target` في `/api/upload`.
- مشغّل الدرس يفهم روابط Drive بكل صيغها ويحوّلها لصفحة معاينة قابلة للتضمين.
- **مهم — لماذا لم تكن الصور تظهر:** جوجل تحجب ملفات Drive عندما يرسل المتصفّح ترويسة `Referer` (حماية من الاقتباس)، فالرابط يعمل من الخادم ولا يظهر داخل الموقع. الحلّ المطبَّق: **وسيط على نطاقنا** `GET /api/media/[id]` يجلب الملف من Drive برمز التطبيق ويمرّره للمتصفّح (مع دعم Range للفيديو والصوت وتخزين مؤقّت يوماً). الرفع الجديد يعيد `/api/media/{id}`، و`lib/media.ts` → `mediaSrc()` يحوّل **أي رابط Drive قديم مخزَّن** إلى الوسيط وقت العرض فلا حاجة لأي ترحيل. لا حاجة حتى لمشاركة الملف علناً.
- **حدود يجب معرفتها:** Drive ليس استضافة فيديو متخصّصة — هناك حصص مشاهدة يومية للملفات العامة ولا يوجد بث تكيّفي (HLS). للفيديوهات الثقيلة يبقى Bunny Stream أو يوتيوب (غير مُدرج) أفضل، والمشغّل يدعمهما أصلاً.

## 5-ج) صلاحية مشاهدة البث
- لكل جلسة `audience`: **`subscribers`** (المشتركون فقط — اشتراك ساري على الكورس المحدّد في `subjectId` أو أي اشتراك) أو **`all`** (كل الطلاب المسجّلين حتى غير المشتركين). الزائر بلا حساب لا يرى أي بث إطلاقاً.
- القاعدة في `lib/access.ts` → `liveVisible(user, live)`، وتُفرَض **على الخادم** داخل `getScopedDB`: الجلسة تظهر في القائمة لكن **الرابط يُرسل فارغاً** لمن لا يحقّ له، مع فلترة إضافية بالصف/الشعبة. الواجهة تستخدم نفس الدالة لعرض القفل و«اشترك للانضمام».
- يمكن تغيير جمهور أي جلسة لاحقاً من بطاقتها في `/admin/live`.

## 5-هـ) الاختبارات داخل المنصّة (بلا روابط خارجية)
- **البناء في `/admin/exams`**: عنوان · كورس مرتبط (اختياري) · صف/شعبة · مدّة بالدقائق · نسبة نجاح · عدد المحاولات (٠ = مفتوح) · الجمهور (مشتركون/كل الطلاب) · حالة (منشور/مجدول)، ثم **أسئلة اختيار من متعدّد** بدرجة لكل سؤال وترتيب قابل للتحريك.
- **الطالب** يحلّه في `/student/exams/[id]`: مؤقّت تنازلي مع **تسليم تلقائي عند انتهاء الوقت**، تنقّل بمربّعات الأسئلة، شريط تقدّم، وبعد التسليم: حلقة نتيجة + مراجعة كاملة تُظهر الصحيح مقابل إجابته.
- **التصحيح على الخادم فقط** (`gradeExam` + `POST /api/exam`): الإجابات الصحيحة تُستبدل بـ`-1` في حمولة الطالب ولا تصل إلا بعد التسليم. الأسئلة نفسها **تُحجب كلياً** عمّن لا يحقّ له الاختبار (`examAllowed`: الجمهور + الصف/الشعبة + الاشتراك).
- الإحصاءات تُحسب من **أفضل محاولة لكل طالب** (`submissions` و`avg`)، وللأدمن جدول نتائج داخل بطاقة كل اختبار (الطالب · الدرجة · النسبة · ناجح/راسب · عدد المحاولات).
- `Exam.questions` صار **مصفوفة أسئلة** بدل عدد؛ `getDB()` يطبّع أي بيانات قديمة تلقائياً. حقل `url` باقٍ للتوافق مع أي اختبار خارجي قديم.

## 5-و) ربط الحساب بجهاز واحد
- `lib/device.ts`: لكل متصفّح معرّف جهاز عشوائي في كوكي **httpOnly موقّعة بـHMAC** (`emz_device`، ٥ سنوات) — لا يُقرأ من الواجهة ولا يُزوَّر.
- **أول تسجيل حساب أو أول دخول يربط الجهاز** بالطالب (`user.deviceId` + وصف الجهاز + التاريخ). أي دخول من جهاز آخر يُرفض بـ٤٠٣ ورسالة: «هذا الحساب مسجَّل على جهاز آخر…».
- **حساب الأدمن غير مقيّد** — يدخل من أي جهاز.
- **السماح بجهاز جديد**: زر «جهاز جديد» في صف الطالب داخل `/admin/students` → `PATCH /api/users {id, action:"resetDevice"}` (أدمن فقط) يفكّ الارتباط، فيرتبط الحساب بالجهاز التالي الذي يسجّل الدخول منه. العمود يعرض وصف الجهاز المرتبط أو «بانتظار أول جهاز».
- ملاحظة تشغيلية: مسح كوكيز المتصفّح أو تغيير الجهاز يُنشئ معرّفاً جديداً، فيحتاج الطالب ضغطة «جهاز جديد» من الأدمن — وهذا هو المقصود من التقييد.

## 5-ز) البث المجاني · قناة اليوتيوب · بطاقات الكورسات · ضبط صورة الهيرو
- **بث مجاني للجميع:** جمهور ثالث `audience: "public"` — يفتحه أي زائر بلا حساب، ويظهر تلقائياً في قسم «بث مجاني» على الصفحة الرئيسية (`components/sections/free-live.tsx`) ويختفي إن لم توجد جلسة. الزائر يرى **هذه الجلسات وحدها** في حمولته (`publicLives`).
- **قناة اليوتيوب (`/admin/youtube`):** ربط القناة برابطها أو `@اسمها` أو `UC…` · جلب الفيديوهات ومزامنتها · التحكّم الكامل: إظهار/إخفاء لكل فيديو، تثبيت، إعادة ترتيب، **إضافة أي فيديو كدرس داخل كورس** بضغطة · إعدادات قسم الصفحة الرئيسية (`components/sections/channel-videos.tsx`) بعنوان ووصف قابلين للتحرير ومشغّل منبثق. المصدر: **YouTube Data API** إذا ضُبط المفتاح — **من متغيّر `YOUTUBE_API_KEY` أو بلصقه مباشرة في صفحة اللوحة** (يُحفظ في `integrations.youtubeApiKey` ولا يُرسل للواجهة أبداً، فقط علم وجوده) — فتُجلب كل الفيديوهات بالصفحات وإلا **تغذية RSS** الرسمية (آخر ١٥ فيديو، بلا أي إعداد). المخفي لا يصل المتصفّح أصلاً.
- **بطاقات الكورسات (`components/brand/course-art.tsx`):** لوحة غلاف SVG كاملة — بغلاف مرفوع تُقصّ الصورة داخل شكل قوسي بحدّ زخرفي وحجاب تدرّجي، وبلا غلاف تُولَّد زخرفة هندسية ثابتة من معرّف الكورس (٤ أنماط × ٦ زوايا) فلكل كورس هوية بصرية مميّزة. حلقة التقدّم وشارة القفل مرسومتان داخل نفس الـSVG.
- **ضبط غلاف الكورس:** `Subject.coverFit` (نفس نوع `ImageFit` المشترك) — من `/admin/courses/[id]` ← بطاقة «غلاف الكورس»: **معاينة حيّة ببطاقة الطالب نفسها** + طريقة الملء (ملء/كامل) + تكبير + إزاحة أفقية ورأسية + إعادة ضبط، ويُحفظ فوراً. يُطبَّق داخل SVG اللوحة بتحويل من المركز بلا تشويه.
- **ضبط صورة الهيرو:** «تخصيص الموقع ← إطار الصورة» فيه الآن معاينة حيّة + طريقة الملء (ملء/كامل) + تكبير + إزاحة أفقية ورأسية (`content.hero.image`)، تُطبَّق داخل الـSVG بتحويل من المركز بلا تشويه.
- **إصلاحات بوابة البث:** كانت أزرار «التشغيل» و«ذكّرني» صمّاء بلا وظيفة، والجلسة تظهر مكرّرة (بطاقة + قائمة)، ولا سبيل لفتح رابط جلسة مجدولة. الآن: ترتيب (مباشر ← قادم بالموعد ← مسجّل)، بطاقة صدارة قابلة للضغط فعلياً، عدّاد «يبدأ بعد …»، «ذكّرني» يضيف الجلسة لتقويم جوجل فعلاً، وشارات «مجاني/للمشتركين».

## 5-ح) الخصومات وألوان الخطط
- `SitePlan` أصبح فيه `color` (لون الخطة) و`cta` (نص الزر) و`discount` = `{active, type: "percent"|"amount", value, label?, until?}`.
- **الحساب في مكان واحد**: `lib/plans.ts` → `planPrice(plan)` يعيد السعر بعد الخصم والأصلي وقيمة التوفير والنسبة، ويُلغي الخصم تلقائياً بعد `until`. يستخدمه: قسم الخطط في الرئيسية، نافذة الشراء في بوابة الطالب، وبطاقات لوحة الأدمن.
- **لوحة الأدمن (`/admin/plans`)**: منتقي لون (٨ ألوان جاهزة + لون مخصّص)، وصندوق خصم (نوع/قيمة/نص العرض/تاريخ الانتهاء) مع معاينة فورية للسعر النهائي، ومؤشّر لون بجانب اسم كل خطة.
- **تصميم الخطط في الرئيسية**: بطاقات تسعير بترويسة SVG (قوس + زخرفة بلون الخطة) · شريط خصم مائل بالنسبة · السعر القديم مشطوباً · «وفّرت كذا ج.م» · عدّاد «ينتهي العرض بعد …» · الخطة المميّزة ترتفع وتُحاط بهالة لونها · تخطيط يتكيّف مع عدد الخطط (١/٢/٣+).

## 5-ط) الفصلان الدراسيان · البث الحقيقي · الجلسة الدائمة · فايربيز
- **الفصل الدراسي:** `Subject.term (1|2)` يُضبط من `/admin/subjects`. بوابة الطالب تعرض **تبويبَي الفصل الأول والثاني** بعدّاد كورسات لكل فصل.
- **اشتراك فصل كامل:** نطاق جديد للخطة `scope: "term"` + `termNo`. الاشتراك يُخزَّن بمفتاح `T1`/`T2`، و`courseActive` يفتح كل مواد ذلك الفصل فقط. المساعد `userOwnsSubject()` على الخادم يمرّر فصل الكورس تلقائياً في كل الفحوص (التقدّم، الاختبارات، الدروس، البث).
- **الاشتراك بعد الدخول فقط:** زر الخطة في الصفحة الرئيسية يذهب لـ`/login?next=/student/subjects` للزائر، ونصّه «سجّل الدخول للاشتراك».
- **بث حقيقي بدل الاجتماع:** `Live.kind = "broadcast" | "meeting"` يُختار عند الإنشاء (بث للمشاهدة فقط / اجتماع تفاعلي).
- **`/api/live`**: `PATCH {id,status}` — «مباشر» **يُرسل إشعاراً فورياً لأجهزة الجمهور** ويسجّله داخل المنصّة، و«منتهي» **يقطع الرابط حالاً** ويحذف حدث جوجل فيُغلق الاجتماع فعلياً. `DELETE` ينهي الجلسة عند جوجل ثم يحذفها. أزرار «ابدأ البث وأشعِر الطلاب» و«إنهاء البث الآن» في بطاقة كل جلسة.
- **جلسة دائمة:** كوكي الجلسة صارت **سنة** وتُجدَّد مع كل تحميل صفحة (`touchSession`) — الطالب لا يخرج من حسابه. **مهم:** علم `Secure` لا يُشتق من بيئة التشغيل بل من `COOKIE_SECURE=1` (يُفعَّل عند النشر على HTTPS فقط) — لأن كوكي Secure على HTTP لا تصل أبداً فتُفقد الجلسات.
- **قناة اليوتيوب صارت إدارية بحتة:** لا تظهر في الصفحة الرئيسية ولا لبوابة الطالب، ولا تغادر بياناتها لوحة الأدمن.
- **Firebase Realtime Database (`/admin/database`):** `lib/firebase.ts` يتحدّث مع RTDB عبر REST من الخادم بمصادقة **حساب خدمة** (JWT موقّع → رمز وصول) أو سرّ قاعدة البيانات. كل حفظ محلي **يُنسخ فوراً** إلى `platform/` في فايربيز (`mirrorToFirebase`)، و`hydrateFromFirebase()` يستورد النسخة السحابية. الصفحة تعرض الحالة وعدد السجلات وأزرار (اختبار الاتصال · رفع · استيراد) وخطوات الإعداد.
  **المطلوب لتفعيله:** `FIREBASE_DATABASE_URL` + `FIREBASE_CLIENT_EMAIL` + `FIREBASE_PRIVATE_KEY` في `.env.local` من ملف حساب الخدمة. المتصفّح لا يتحدّث مع فايربيز إطلاقاً، فتبقى قواعده مغلقة تماماً.

## 5-ي) التخزين السحابي الكامل · النسخ الاحتياطي · روابط الدعم
- **Firebase Realtime Database هي قاعدة البيانات الفعلية**: `lib/store.ts` طبقة تخزين بذاكرة مؤقّتة (١٥ ثانية) تقرأ من فايربيز عند كل طلب (`loadDB()` في بداية كل مسار API وفي `app/layout.tsx`)، وكل تعديل يمرّ بطابور كتابة مرتّب (`commit`) وتنتظره المسارات المعدِّلة عبر `flushDB()` قبل الردّ.
- **لا شيء محلي:** لا يُكتب ملف على الخادم إطلاقاً وقت تفعيل السحابة، وملفات الوسائط كلها على Drive (`/api/media/[id]` وسيط النطاق). زر «نقل الملفات إلى Drive» نقل ما كان متبقّياً وحدّث روابطه تلقائياً.
- **ترميز المفاتيح:** فايربيز تمنع `. $ # [ ] /` في المفاتيح، وبياناتنا فيها مثل `hero.statusPill` — تُرمَّز عند الكتابة وتُفكّ عند القراءة (`encodeForFirebase`/`decodeFromFirebase`)، كما تُعاد المصفوفات لشكلها الصحيح عند القراءة.
- **حماية من فقدان البيانات:** إذا تعذّر الوصول للسحابة لا يُزرع محتوى فارغ فوقها — يُستخدم آخر ما في الذاكرة أو يُفشل الطلب بوضوح.
- **النسخ الاحتياطي (`/admin/backup`):** نسخة **تلقائية كل ٢٤ ساعة**، ونسخة يدوية بضغطة تُحفظ **على جهازك + Google Drive + السحابة**، وسجلّ بآخر النسخ، و**استعادة** من ملف على جهازك أو من أي نسخة في Drive (مع نسخة أمان تلقائية قبل الاستبدال والحفاظ على ربط جوجل). يُحتفظ بآخر ٣٠ نسخة في Drive و٧ لقطات في السحابة.
- **قواعد الحماية:** `firebase/database.rules.json` — قواعد مغلقة بالكامل (`.read/.write = false`) مع تحقّق بنيوي موثّق. الخادم يتجاوزها بحساب الخدمة، فلا يستطيع أي متصفّح قراءة بيانات الطلاب.
- **روابط الدعم (`/admin/support`):** واتساب/هاتف/بريد + **روابط مخصّصة غير محدودة** (تليجرام، فيسبوك، يوتيوب، أي رابط) بعنوان ووصف وترتيب وإظهار/إخفاء، وسطر توضيحي — تظهر كلها في صفحة «المساعدة» عند الطالب.
- **غلاف الكورس — لا خيار يقصّ إطلاقاً:** `ImageFit` صار فيه `frame` و`shape` و`radius`:
  · **«الإطار يتبع الصورة»** — ارتفاع اللوحة يُحسب من نسبة الصورة الأصلية (`Subject.coverRatio` تُقاس تلقائياً عند تعيين الغلاف) فتملأ الإطار تماماً بلا قصّ ولا هوامش.
  · **«إطار ثابت»** — نسبة موحّدة لكل البطاقات، والصورة كاملة في وسطه بهامش أمان ١٢ وحدة، والفراغ يُملأ بالزخرفة الهندسية.
  · **شكل الحواف**: قوس / دائرية / مستقيمة + منزلق انحناء (٠–٤٨). وخيار «ملء البطاقة (يقصّ)» أُزيل نهائياً.
- **بلا قصّ للصور إطلاقاً:** إطار الهيرو وغلاف الكورس يعرضان الصورة **كاملة كما رُفعت**. ثلاث نقاط قصّ كانت موجودة وأُزيلت: `preserveAspectRatio` على جذر لوحة الغلاف كان `slice`، وتكبير البطاقة عند التمرير داخل حاوية `overflow-hidden`، وقوس اللوحة الذي كان يقصّ الأركان — الآن تُرسم الصورة داخل هامش أمان ١٢ وحدة فلا يلمسها الإطار، والهوامش تُملأ بالزخرفة الهندسية بلون الثيم فتبدو مقصودة. خيار «ملء البطاقة (يقصّ)» ما زال متاحاً لمن يريده.

## 5-ك) طبقة الحماية
- **`middleware.ts`** (يعمل قبل كل طلب): ترويسات صارمة — CSP محدّدة المصادر · `X-Frame-Options` · `nosniff` · `Referrer-Policy` · `Permissions-Policy` · `COOP/CORP`؛ وصدّ فحص المسارات المعروفة (`.env`, `wp-admin`, `.git`…) بـ٤٠٤؛ ورفض أي طلب معدِّل من أصل خارجي (CSRF). كل ما يُصدّ يُحوَّل إلى `/api/security/report` ليُدوَّن في السجلّ.
- **`lib/guard.ts`**: تحديد معدّل بالذاكرة · `isTrustedIp` (لا تُحظر الحلقة المحلية والشبكة الداخلية أبداً) · فحص الأصل · قوّة كلمة المرور · صحّة البريد.
- **سياسة الدخول:** حدّ صلب ١٢٠ محاولة/ساعة لكل عنوان (حماية من الإغراق) + حدّ ليّن (٨/دقيقة، و٦/٥ دقائق لكل حساب من نفس العنوان). **البيانات الصحيحة تمرّ دائماً** حتى أثناء التقييد — فلا يستطيع مهاجم قفل صاحبة المنصّة خارج لوحتها بمحاولات فاشلة متعمّدة.
- **التسجيل الذاتي:** ٣ حسابات/ساعة لكل عنوان · بريد صحيح إلزامي · كلمة مرور ٨ أحرف على الأقل بحرف ورقم وليست من القائمة الشائعة.
- **`lib/security.ts` + `/admin/security`:** كل محاولة مشبوهة تُسجَّل (النوع، العنوان، المتصفّح، الحساب، التفاصيل، الخطورة) وتُحفظ مع البيانات في فايربيز. **حظر تلقائي** لعنوان تكرّرت منه ٨ محاولات خطيرة خلال ١٠ دقائق، وحظر/رفع حظر يدوي من اللوحة.
- **الجلسة:** انتهاء صلاحية حقيقي داخل الرمز نفسه (لا يُقبل رمز أقدم من سنة حتى لو بقيت الكوكي)، وتحقّق من الدور.
- **وسيط الوسائط:** لا يخدم إلا ملفاً **مذكوراً فعلاً** في بيانات المنصّة — فلا يتحوّل الخادم إلى وسيط مفتوح لبقية ملفات Drive.
- **اختبار آلي (`test_security.py`) — ٢٨ فحصاً كلها ناجحة:** الترويسات · صدّ الفحص الآلي · CSRF · عزل بيانات الزائر · حجب المسارات الإدارية · رفض كلمات المرور الضعيفة · تقييد التخمين · تسجيل كل ذلك · الحظر ورفعه.

## 6) الميزات المنفّذة (كلها مُختبَرة وتعمل)
**الصفحة الرئيسية:** Navbar عائم + لايت/دارك للزائر · Hero بـ٨ فريمات · الأقسام: لماذا نحن، **الخطط** (بدل قائمة المواد — **المواد لم تعد تظهر للزائر إطلاقاً**)، الأسئلة، CTA + فوتر · **زر «شاهد درساً مجانياً» لا يعمل إلا بعد تسجيل الدخول والرابط يأتي من السيرفر**.

**طبقة التصميم المتّجهة (SVG):** واجهة الموقع العامة وصفحات الدخول/التسجيل مرسومة بالكامل بـSVG يدوي في `components/brand/`:
`logo.tsx` (علامة الخاتم الثماني) · `pattern.tsx` (`GeoBackdrop` تبليط هندسي بقناع تلاشٍ، `RuleOrnament`، `CornerKnot`، `ArchTile`، `archPath`) · `icons.tsx` (كل الأيقونات على شبكة ٢٤ وسُمك ١٫٥ — لا مكتبة أيقونات في الواجهة العامة، وخاصية `anim` تضيف حركة: `draw` رسم عند الظهور · `swing` تأرجح الجرس · `pulse` نبض البث · `flick` اهتزاز اللهب · `bob` طفو · `pop`/`tick` ظهور نطّي · `ico-slide` انزلاق السهم عند التمرير — وكلها داخل `prefers-reduced-motion: no-preference` فتتوقّف تماماً عند تقليل الحركة) · `illustrations.tsx` (رسوم الحالات الفارغة) · `use-uid.ts` (معرّفات defs فريدة ومنظّفة حتى لا تتصادم النُسخ).
`hero-frames.tsx` أُعيد بناؤه: ٨ إطارات SVG (قوس محراب، مدالية، خاتم ثماني، مثمّن، قوس مفصّص، طاق معقود، حلقات مدارية، طاق مزدوج) بقصّ `clipPath` وصورة `<image>` داخل الـSVG، وكلها ٤٠٠×٥٠٠ فلا يتغيّر التخطيط عند تبديل الإطار، والحركة تتوقّف مع `prefers-reduced-motion`.
حُذفت الهالات الضبابية (`blur-2xl/3xl`) ووميض الأزرار وحركة التدرّج اللانهائية، وصار التدرّج النصّي بلونين ثابتين.

**قسم الخطط (`/admin/plans`):** إضافة/تعديل/حذف خطط بأسمائها وأنواعها ونطاقها وأسعارها ومدّتها ومزاياها، إظهار/إخفاء وترتيب وإبراز. نصوص القسم (عنوان/وصف/ملاحظة) من `/admin/customize`.

**لوحة الأدمن:** نظرة عامة (الإيراد من أسعار الخطط) · تخصيص · الطلاب · الصفوف · الكورسات · **الخطط** · الأكواد (تُولَّد من خطة) · الاختبارات · **البث (اجتماع Google Meet بضغطة + تحديد الجمهور)** · **الإشعارات (لكل الطلاب أو صف/شعبة أو طالب بعينه)** · التحليلات · الدعم.
**تصميم اللوحة:** سكِن خاص (`.admin-skin` في `globals.css` + غلاف في `app/admin/layout.tsx`): خلفية كريمية، سايدبار أخضر بكبسولة بيضاء للعنصر النشط، لوح محتوى أبيض بحواف دائرية، بطاقات بيضاء ناعمة — **لا يؤثر على الموقع أو بوابة الطالب**.

**إدارة الكورس (`/admin/courses/[id]`):** **كل المحتوى بالرابط** — رابط الفيديو (YouTube/Bunny/Vimeo/mp4)، رابط الملفات (Drive/PDF)، رابط الغلاف (مع إبقاء رفع الصورة اختيارياً) · **اختبار تفاعلي اختياري لكل فيديو**: تشغيل/إيقاف، نسبة نجاح، أسئلة اختيار من متعدّد بتحديد الإجابة الصحيحة، وإحصاء محاولات الطلاب ومتوسّط الدرجات.

**التطبيق المثبّت (PWA):** `app/manifest.ts` (يقرأ الاسم والشعار واللون من قاعدة البيانات) · `app/api/pwa-icon` يولّد أيقونات PNG (192/512/maskable) وقت الطلب عبر `next/og` من شعار المنصّة أو علامة الخاتم · `public/sw.js` (**لا يخزّن أي `/api/*` ولا صفحات — أصول البناء وصفحة «بلا اتصال» فقط، حتى لا تتسرّب بيانات طالب لآخر على جهاز مشترك**) · `components/pwa/register-sw.tsx` · `components/pwa/install-app.tsx` (زر تثبيت حقيقي عبر `beforeinstallprompt`، وخطوات «مشاركة ← إضافة للشاشة الرئيسية» على iOS، ويختفي إذا كان مثبّتاً). يظهر في رئيسية الطالب وصفحة المساعدة. **التثبيت يتطلّب HTTPS في الإنتاج** (localhost مستثنى).

**إشعارات على شاشة الجهاز (Web Push):** `lib/push.ts` (مكتبة `web-push` + مفاتيح VAPID في `.env.local`) · `POST /api/notifications` ينشئ الإشعار **ويدفعه فوراً** لأجهزة جمهوره (نفس قاعدة الاستهداف: طالب/صف/شعبة) ويعيد عدد الأجهزة التي وصلته · `/api/push/subscribe` (GET المفتاح العام · POST تسجيل جهاز · DELETE إلغاء) · معالجا `push` و`notificationclick` في `public/sw.js` (النقر يُركّز نافذة مفتوحة أو يفتح جديدة على صفحة الإشعارات) · بطاقة «تفعيل الإشعارات» في رئيسية الطالب وصفحة الإشعارات (الإذن لا يُطلب إلا بضغطة). **اشتراكات الأجهزة لا تُرسل لأي واجهة** — `toPublicUser` يستبدلها بعدد `pushDevices` فقط، والاشتراكات الميتة (٤٠٤/٤١٠) تُحذف تلقائياً. **على iOS لا تعمل الإشعارات إلا بعد تثبيت التطبيق** على الشاشة الرئيسية (تُعرض الرسالة تلقائياً).

**اختصارات التطبيق:** أربعة اختصارات (الكورسات · البث · الاختبارات · الإشعارات) بأيقونات PNG مولّدة من `/api/shortcut-icon` بنفس رموز الهوية ولون الثيم — تظهر عند الضغط المطوّل على أيقونة التطبيق.

**تصميم الجوّال:** `viewport-fit=cover` + حوّاف آمنة (`env(safe-area-inset-*)`) في الشريط العلوي والسفلي والدرج · شريط تنقّل سفلي شبكي بمؤشّر متحرّك وأهداف لمس ≥ ٤٤px · نافذة الشراء تتحوّل إلى **ورقة سفلية** على الجوّال · مشغّل الدرس بعرض الشاشة الكامل · حقول الإدخال 16px (منع تكبير iOS) · إلغاء وميض اللمس · `overscroll-behavior` في وضع التطبيق.

**بوابة الطالب (SVG بالكامل مثل الموقع):** الرئيسية (**حلقة تقدّم SVG + بطاقات اشتراكاته والمتبقّي من أيامها**) · الكورسات (فلترة صف/شعبة + نافذة شراء تعرض **خطط الأدمن** + كود التفعيل + حالة الاشتراك) · مشغّل الكورس (**اختبار تفاعلي بعد الفيديو** بتصحيح من السيرفر ونتيجة ونجاح/رسوب وإعادة محاولة) · الاختبارات · البث · **الإشعارات (جديد/مقروء + نقطة في الشريط)** · المساعدة.

**الأمان (بلا ثغرات معروفة):** لا تسريب لأي بيانات خارج نطاق صاحب الجلسة · الأكواد لا تصل للمتصفّح أبداً · روابط الدروس المدفوعة لا تُرسل لغير المشترك · إجابات الاختبارات لا تغادر السيرفر · انتهاء الاشتراك يُطبَّق على كل المسارات · الدرس المجاني خلف تسجيل الدخول · فصل أدوار كامل.

## 7) بنية الملفات المهمة
- `lib/`: `types.ts` (+`SitePlan`/`Quiz`/`QuizResult`) · `defaults.ts` (بذور فارغة + هوية إيمان) · `db.ts` (+`getScopedDB`/`gradeQuiz`) · `access.ts` (+`planExpiry`/`activeSubs`/`daysLeft`) · `session.ts` · `data.ts` (navLinks + محافظات مصر + الشعب) · `dashboard-data.ts` (تنقّل اللوحات) · `ui-style.ts` (ألوان/إظهار العناصر) · `seo.ts`.
- `components/content/content-provider.tsx` — **المزوّد المركزي** (يجلب المحتوى، يحفظ، يرفع، يطبّق الثيم، الجلسة، wa، لايت/دارك). كل المكوّنات تقرأ منه عبر `useContent()`. الجذر `app/layout.tsx` يمرّر `initialDB` و`initialSession` (SSR).
- `components/sections/*` — أقسام الهبوط · `plans.tsx` (قسم الخطط) · `hero-frames.tsx` (الـ8 فريمات). (`curriculum.tsx` حُذف — المواد لا تظهر للزائر.)
- `components/dashboard/{shell,ui}.tsx` — هيكل اللوحات + مكوّنات.
- `components/ui/*` — primitives, skeleton, route-transition, video-modal, animated-icons.

---

## 8) الحالة الحالية / أفكار للمتابعة
- ✅ كل ما سبق مبنيّ ومُختبَر (بناء ناجح + ٤٤ اختبار تكامل + ٢٨ اختبار حماية آلية) ومنشور على Vercel.
- ✅ **التخزين السحابي مُنفَّذ بالفعل عبر Firebase** (لم تعد Supabase مطلوبة — الفكرة نُفِّذت على Firebase).
- أفكار مفتوحة: تصدير بيانات الطلاب (Excel/CSV)، معاينة حيّة داخل صفحة التخصيص، بنك أسئلة عام للاختبارات.

## 8-ب) نسخة ثانية لمدرّسة أخرى
- توجد نسخة مطابقة للكود في `C:\Users\pc\Downloads\eman-zedan-teacher2` (بدون git/vercel/بيانات/أسرار) — تعمل محلياً فوراً بتخزين ملف محلي.
- لجعلها منصّة مستقلّة منشورة: أنشئ لها `.env.local` من `.env.example` بقيمها الخاصة + مشروع Firebase + مشروع Vercel + ربط Google خاص بها، وحدّث `.firebaserc`. **لا تُشارك أسرار المنصّة الأولى** (تفادياً لاختلاط البيانات). اسم المدرّسة وكل الهوية تُضبط من `/admin/customize` (بيانات لا كود).

## 9) ملاحظات تشغيل مهمة
- بعد أي تعديل: `npm run build` ثم `npm start -- -p <port>` (الإنتاج) — على ويندوز أوقف Node عبر PowerShell: `Get-Process node | Stop-Process -Force`.
- عند اختبار الـAPI بـ curl على ويندوز: **العربية تتلف في `curl -d`** — استخدم Python client (UTF-8) للاختبار.
- الفلترة (صف/شعبة) والإشعارات تعتمد على الجلسة في SSR (تُمرَّر من `app/layout.tsx`).
- احذف `data/db.json` لإعادة الحالة لمنصة فارغة نظيفة.

---

## 10) المنصّة الأم (Multi-Tenant) — المرحلة M1: سياق المستأجر

> الخطّة الكاملة في `SAAS-PROMPT.md`. هذه المرحلة تجعل الكود **متعدّد المنصّات** بلا أي تغيير مرئي: المنصّة الحالية تعمل كما هي على الجذر وعلى نطاقها الفرعي.

### كيف يُعرف المستأجر
- **الوسيط `middleware.ts`** يصنّف المضيف عبر `lib/hub/resolve.ts` (نقيّ، يعمل على Edge): `{slug}.{ROOT_DOMAIN}` أو `{slug}.localhost` = منصّة · الجذر/`www`/`*.vercel.app`/`localhost` = جذر · غير ذلك = دومين مخصّص (يُخدم كجذر حتى M6). يضع `x-host-kind` و`x-tenant-slug` **بعد محو أي ترويسة بهذا الاسم من العميل** (لا انتحال). مساراتُ الـHub (`/hub`, `/start`, `/api/hub`) على نطاق منصّة → ٤٠٤. محلّياً: `?tenant=slug` يثبّت كوكي `dev_tenant` (لا يعمل على فيرسل).
- **السجلّ `lib/hub/registry.ts`**: `hub/slugs/{slug}` → `tenantId`، و`hub/tenants/{id}` بطاقة المنصّة (`lib/hub/types.ts`)، بذاكرة مؤقّتة دقيقة ونفي ١٥ ثانية. بلا فايربيز يُقرأ `data/hub.json`. المنصّة الافتراضية = `DEFAULT_TENANT_ID` (وإلا `default`)، ويخدمها الجذر ما دام `ROOT_HOST_MODE=tenant`.
- **السياق `lib/hub/context.ts`**: `AsyncLocalStorage` لمسارات API (كلها ملفوفة بـ`tenantRoute()` — ٣٨ ملفاً)، وصندوق `cache()` من React لمكوّنات الخادم يملؤه `bindTenant()` الذي يستدعيه `loadDB()`. **`getDB()` بلا سياق يرمي `TenantContextMissing`** — لا بيانات افتراضية أبداً. `runInTenant(ctx, fn)` للمهامّ المجدولة.

### أين تعيش البيانات
| كان | صار |
|---|---|
| `platform/` | `tenants/{id}/platform` |
| `backups/` · `activity/` · `claims/` · `decisions/` | `tenants/{id}/…` |
| `data/db.json` | `data/tenants/{id}/db.json` |
| — | `hub/tenants` · `hub/slugs` (+ `data/hub.json` محلّياً) |

`lib/store.ts` صار لكل منصّة مخبأٌ وطابور كتابة وجذر (LRU ٥٠ منصّة في الذاكرة). **المنصّة الافتراضية تقرأ الجذر القديم `platform/` إن كان جذرها الجديد فارغاً** (قراءة فقط) فلا تنقطع بين النشر والترحيل؛ أوّل حفظ يكتب الجديد.

### الجلسة والحماية
- رمز `emz_session` يحمل `tid`؛ يُرفض على منصّة أخرى. الرمز القديم بلا `tid` يُقبل على الافتراضية وحدها ويُستبدل بموسوم في أوّل زيارة (`touchSession`).
- مفاتيح حدود المعدّل في `lib/guard.ts` تُسبَق بمعرّف المنصّة. سجلّ الأمان والحظر داخل قاعدة كل منصّة أصلاً.
- `firebase/database.rules.json`: جذرا `tenants` و`hub` مغلقان بالكامل مع تحقّق بنيوي (نسخة قواعد `platform` تُطبَّق على `tenants/$id/platform`).

### الترحيل والتشغيل
```bash
node scripts/migrate-to-tenants.mjs --dry          # يعرض ما سيفعله
node scripts/migrate-to-tenants.mjs --id default   # ينسخ platform/ → tenants/default/platform ويسجّل المنصّة في hub/ — لا يحذف شيئاً
```
- `npm run start:local` / `npm run dev:local`: تشغيل محلّي **بلا فايربيز** (يُفرغ مفاتيحها) على `data/` — لا يلمس الإنتاج.
- `npm run test:tenancy` (بعد `node scripts/test-tenancy.mjs --seed` مرّة ثم تشغيل الخادم): ١٨ فحص عزل — جذر/فرعي، ترويسة منتحلة، مضيف مجهول، مسارات Hub، كوكي منقولة بين منصّتين، انفصال البيانات. **كلها ناجحة.**
- `/api/cron/backup` يمرّ على كل المنصّات النشطة/الموقوفة بدفعات.

### ما يلي (M2+)
`tenantGate` (إيقاف/انتهاء)، لوحة الـHub، إخفاء الأقسام، Onboarding بجوجل، الفوترة، الدومين المخصّص — بالترتيب في `SAAS-PROMPT.md` §12.

## 11) مشغّل يوتيوب النظيف
`components/student/clean-youtube.tsx`: مقطع يوتيوب يُعرض **بلا عنوان ولا شعار ولا مقترحات ولا أزرار يوتيوب** — الإطار أصمّ (`controls=0` + `pointer-events:none`)، غطاء معتم كلّما لم يكن المقطع شغّالاً (قبل البدء/الإيقاف/الانتهاء — لحظات ظهور واجهة يوتيوب)، والتحكّم كلّه بأزرارنا عبر `postMessage` (بلا سكربت خارجي ولا تغيير في CSP). يُستعمل في مشغّل الدرس (`unit-view.tsx`) ونافذة الدرس المجاني (`video-modal.tsx`). العلامة المائية تبقى فوق المقطع.
