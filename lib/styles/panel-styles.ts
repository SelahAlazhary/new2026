/**
 * معالجةُ لوح الترحيب — المحورُ الثاني.
 * ------------------------------------------------------------------
 * في `lib/designs.ts` اثنتان وعشرون «هيئة»، وكلُّها **اختلافُ حافّةٍ لا
 * غير**: قصٌّ في الأركان، أو قوسٌ فوق، أو أسنانٌ تحت. واللوحُ نفسُه في
 * جميعها كتلةٌ كحليّةٌ متدرّجةٌ بنصٍّ أبيض — فمن بدّل بينها لم يرَ فرقاً
 * يُذكر، وظنّ الخيارَ لا يعمل.
 *
 * **والفرقُ الذي يُرى ليس في الحافّة**: هو في السطح (داكنٌ أم فاتح)،
 * وفي التخطيط (صفٌّ أم عمود)، وفي ثقل الخطّ، وفي موضع الحدّ.
 *
 * فهذا محورٌ ثانٍ يُضرب في الأوّل: ثمانيُ معالجاتٍ × اثنتين وعشرين هيئة.
 * وكلُّ معالجةٍ **تغيّر ما يُرى فعلاً**، لا ما يُقاس بالمسطرة.
 *
 * **ولا لونَ جديد**: كلُّها من كحليّ الهوية وذهبِها وورقِها. والاختلافُ
 * في توزيعها لا في استبدالها.
 */

export type PanelStyle = {
  id: string;
  name: string;
  hint: string;
};

export const PANEL_STYLES: PanelStyle[] = [
  {
    id: "ink",
    name: "الحبر",
    hint: "لوحٌ كحليٌّ متدرّجٌ بنصٍّ أبيض — الأوقر، وهو ما كان",
  },
  {
    id: "paper",
    name: "الورق",
    hint: "سطحٌ فاتحٌ بنصٍّ كحليٍّ وحدٍّ ذهبيٍّ شعرة — أخفُّ وأهدأ",
  },
  {
    id: "rule",
    name: "الحدّ الجانبيّ",
    hint: "بلا تعبئة، وشريطٌ ذهبيٌّ عريضٌ على الطرف — أنظفُ ما يكون",
  },
  {
    id: "split",
    name: "المقسوم",
    hint: "الترحيبُ في جهةٍ والأرقامُ في أخرى، بينهما فاصلٌ ذهبيّ",
  },
  {
    id: "frame",
    name: "المؤطَّر",
    hint: "لوحٌ داخل حقلٍ ملوَّن — عمقٌ بالتراكب لا بالظلّ",
  },
  {
    id: "band",
    name: "الشريط",
    hint: "شريطٌ ذهبيٌّ يعبر أعلى اللوح ويحمل الاسم",
  },
  {
    id: "bare",
    name: "المجرَّد",
    hint: "بلا لوحٍ أصلاً — عنوانٌ كبيرٌ على ورق الصفحة وخطٌّ تحته",
  },
  {
    id: "duo",
    name: "المزدوج",
    hint: "نصفٌ كحليٌّ ونصفٌ ذهبيّ — الاسمُ على الحدّ بينهما",
  },
  {
    id: "center",
    name: "المتوسّط",
    hint: "كلُّ شيءٍ في الوسط — الاسمُ تحت الحلقة لا بجانبها",
  },
  {
    id: "bar",
    name: "السطر",
    hint: "سطرٌ واحدٌ لا لوح — أقصرُ ما يكون، والشاشةُ للمحتوى",
  },
];

export const DEFAULT_PANEL_STYLE = PANEL_STYLES[0].id;

export function findPanelStyle(id?: string): PanelStyle {
  return PANEL_STYLES.find((x) => x.id === id) ?? PANEL_STYLES[0];
}

/**
 * الصنفُ على جذر بوّابة الطالب.
 * ------------------------------------------------------------------
 * والصنفُ على الجذر لا على اللوح نفسِه: المعالجةُ تمسّ اللوحَ وبطاقاتِه
 * وترتيبَ ما فيه، فلو كُتبت على اللوح وحدَه لَما بلغت ما حوله.
 */
export function panelStyleClass(x: PanelStyle): string {
  return `pnl-${x.id}`;
}
