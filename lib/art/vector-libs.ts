/**
 * مكتباتُ رسوم الموقع — أربعون.
 * ------------------------------------------------------------------
 * الرسمُ في المنصّة ستُّ لوحاتٍ متجهة (المصحف · المسجد · الإجازة ·
 * الفانوس · الرَّحل · الشمسة). ومكتبةُ الرسم **ليست ستَّ لوحاتٍ أخرى**:
 * أربعون طقماً مرسوماً بيدٍ واحدةٍ يعني مئتين وأربعين لوحة، ولا تُرسم
 * مئتان وأربعون لوحةً بجودةٍ واحدة — يخرج منها خمسٌ حسنةٌ وبقيّةٌ حشو.
 *
 * فالمكتبةُ هنا **معالجة**: هندسةٌ واحدةٌ محكمة، وأربعون طريقةً في
 * معالجتها. وهو نفسُه ما تفعله مكتباتُ الأيقونات العشر في هذه المنصّة:
 * الخانةُ واحدةٌ والمعالجةُ تختلف.
 *
 * وتُبنى من ضربِ **ثماني لوحاتِ لونٍ في خمسِ تشطيبات**:
 *
 *   اللوحة  → أيُّ الألوان الثلاثة يكون جسماً، وأيُّها سطحاً، وأيُّها
 *             زخرفة. وتبديلُ الأدوار وحدَه يقلب الرسمَ رأساً على عقب:
 *             كحليٌّ على ورقٍ غيرُ ورقٍ على كحليّ.
 *   التشطيب → ما يُضاف حول الرسم: قرصٌ يجلس عليه، أو هالةٌ تحته، أو
 *             ظلٌّ طويلٌ مائل، أو حدٌّ ذهبيّ.
 *
 * ثمانيةٌ في خمسةٍ أربعون... بل أربعون بالضبط. وكلُّ واحدةٍ منها تختلف
 * عن أختها اختلافاً يُرى من بعيد، لا فرقَ درجةٍ لا يميّزه أحد.
 *
 * **والتطبيقُ بمتغيّرات CSS لا بتفريعٍ في الشيفرة.** الرسمُ يكتب
 * `var(--sv-body)` ولا يعلم أيَّ مكتبةٍ اختيرت؛ والصنفُ على جذر الصفحة
 * يكتب المتغيّر. فتبديلُ المكتبة لا يُعيد رسمَ شيء، ولوحةٌ سابعةٌ تُضاف
 * غداً تتبع الأربعين كلَّها بلا سطرٍ واحد.
 */

export type VectorPalette =
  | "asil"      // الأصل: كحليٌّ جسماً · ورقٌ سطحاً · ذهبٌ زخرفة
  | "maqlub"    // مقلوب: ذهبٌ جسماً على ورق
  | "layli"     // ليليّ: ورقٌ على كحليّ — الرسمُ يضيء في الظلمة
  | "muhabbar"  // محبَّر: كحليٌّ كلُّه بدرجاتٍ منه
  | "dhahabi"   // ذهبيّ: ذهبٌ كلُّه بدرجاتٍ منه
  | "waraqi"    // ورقيّ: خفوتٌ عامّ — يُحسّ ولا يُقرأ
  | "shafaqi"   // شفقيّ: ذهبٌ جسماً وكحليٌّ زخرفةً — قلبُ الدورين
  | "hadd"      // حادّ: تباينٌ أقصى بلا وسط
  | "raqim"     // الرقيم: أسطحُ الرسم ذهبٌ مصمت — كصفحةٍ مذهّبة
  | "sadaf";    // الصدفيّ: جسمٌ ورقيٌّ على أرضٍ ذهبيّة

export type VectorFinish =
  | "flat"      // مسطّح — الرسمُ وحدَه
  | "disc"      // على قرصٍ يجلس عليه
  | "halo"      // بهالةٍ تحته
  | "shadow"    // بظلٍّ طويلٍ مائل
  | "ringed"    // محاطٌ بحدٍّ ذهبيّ
  /* ــ وخمسٌ تُحرّك الرسمَ كلَّه، فوق حركته الداخليّة ــ */
  | "float"     // يطفو صعوداً وهبوطاً
  | "breathe"   // يتنفّس — يكبر ويصغر
  | "tilt"      // يتمايل يميناً ويساراً
  | "spin"      // يدور دورةً بطيئة
  | "beat";     // هالةٌ تنبض تحته

export type VectorLib = {
  id: string;
  name: string;
  palette: VectorPalette;
  finish: VectorFinish;
};

const PALETTES: { key: VectorPalette; name: string }[] = [
  { key: "asil", name: "الأصيل" },
  { key: "maqlub", name: "المقلوب" },
  { key: "layli", name: "الليليّ" },
  { key: "muhabbar", name: "المحبَّر" },
  { key: "dhahabi", name: "الذهبيّ" },
  { key: "waraqi", name: "الورقيّ" },
  { key: "shafaqi", name: "الشفقيّ" },
  { key: "hadd", name: "الحادّ" },
  { key: "raqim", name: "الرقيم" },
  { key: "sadaf", name: "الصدفيّ" },
];

const FINISHES: { key: VectorFinish; name: string }[] = [
  { key: "flat", name: "مسطّح" },
  { key: "disc", name: "على قرص" },
  { key: "halo", name: "بهالة" },
  { key: "shadow", name: "بظلّ" },
  { key: "ringed", name: "بحدّ" },
  { key: "float", name: "يطفو" },
  { key: "breathe", name: "يتنفّس" },
  { key: "tilt", name: "يتمايل" },
  { key: "spin", name: "يدور" },
  { key: "beat", name: "ينبض" },
];

/**
 * المئةُ — تُولَّد بالضرب لا تُكتب يداً.
 *
 * وكتابتُها يداً مئةَ سطرٍ تُخطئ في واحدٍ منها فيتكرّر معرّفٌ أو يسقط
 * تركيب، ولا يظهر الخللُ إلّا حين يختار الأستاذُ ذلك الواحدَ بعينه.
 * والضربُ يضمن أنّها مئةٌ بلا تكرارٍ ولا نقص.
 *
 * **والتشطيباتُ محورٌ واحدٌ لا محوران.** خمسٌ تعالج السطحَ (أرضٌ وهالةٌ
 * وظلٌّ وحدّ)، وخمسٌ تحرّك الرسمَ كلَّه فوق حركته الداخليّة. وجعلُهما
 * محورين مستقلّين يُخرج مئتين وخمسين تركيباً، وأكثرُها لا يُميَّز عن
 * أخيه — والمكتبةُ التي لا تُميَّز عن أختها ليست مكتبة.
 */
export const VECTOR_LIBS: VectorLib[] = PALETTES.flatMap((p) =>
  FINISHES.map((f) => ({
    id: `${p.key}-${f.key}`,
    name: f.key === "flat" ? p.name : `${p.name} · ${f.name}`,
    palette: p.key,
    finish: f.key,
  })),
);

export const DEFAULT_VECTOR_LIB = VECTOR_LIBS[0].id;

export function findVectorLib(id?: string): VectorLib {
  return VECTOR_LIBS.find((x) => x.id === id) ?? VECTOR_LIBS[0];
}

/** الصنفان يُكتبان على جذر الصفحة — واللوحةُ والتشطيبُ مستقلّان. */
export function vectorLibClass(lib: VectorLib): string {
  return `svp-${lib.palette} svf-${lib.finish}`;
}
